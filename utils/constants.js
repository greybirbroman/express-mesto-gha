const STATUS_OK = 200;
const STATUS_CREATED = 201;
const ERROR_CODE = 400;
const FOUND_ERROR_CODE = 404;
const SERVER_ERROR = 500;

module.exports = {
  STATUS_OK,
  STATUS_CREATED,
  ERROR_CODE,
  FOUND_ERROR_CODE,
  SERVER_ERROR,
};
